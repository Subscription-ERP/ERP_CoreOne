<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rootcore.hr.mapper.HrDocumentMapper">

	<!-- 증명서관리 -->
	
	<!-- 사원검색 (조건없을시 전체조회)-->
	<select id="selectUser" resultType="UserVO">
		SELECT 
		    u.user_id,
		    u.user_name,
		    d.dept_name,
		    FN_FIND_CODENAME('0C', u.job_title) AS job_title_name,
		    FN_FIND_CODENAME('0D', u.position) AS position_name
		FROM tb_user_master u
		JOIN tb_dept_master d
		ON u.dept = d.dept_code
		<where>
			<!-- 이름 검색 -->
			<if test="userName != null and userName != ''">
				AND user_name LIKE '%' || #{userName} || '%'
			</if>
			<!-- 사번 검색 -->
			<if test="userId != null and userId != ''">
				AND user_id = #{userId}
			</if>
		</where>
		ORDER BY user_id DESC
	</select>


	<!-- 증명서 등록 -->
	<insert id="insertHrDocument" parameterType="HrDocumentVO">
		<selectKey keyProperty="docCode" order="BEFORE" resultType="String">
			SELECT fn_make_date_code('HR_DOCUMENT') 
			FROM dual
		</selectKey>
	
		INSERT INTO tb_hr_document (
		    company_code,
		    doc_code,
		    user_id,
		    doc_type,
		    issue_date,
		    purpose,
		    created_by,
		    create_date)
		VALUES (
		    #{companyCode},
		    #{docCode},
		    #{userId},
		    #{docType},
		    #{issueDate},
		    #{purpose},
		    #{createdBy},
		    SYSDATE
		)
	</insert>
	
	<!-- 단건조회(증명서 종류) -->
	<select id="selectDocumentByCode" resultType="HrDocumentVO">
		SELECT
		   doc_code,
		   doc_type
		FROM tb_hr_document
		WHERE doc_code = #{docCode}
	</select>
	
	


</mapper>