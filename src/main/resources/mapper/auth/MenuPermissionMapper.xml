<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rootcore.auth.mapper.MenuPermissionMapper">

    <!-- ✅ 1) USER 목록 조회 -->
    <select id="selectUserList" resultType="com.rootcore.auth.vo.UserAuthVO">
    SELECT
        U.COMPANY_CODE,
        U.USER_ID,
        U.USER_NAME,
        U.DEPT,
        U.POSITION,
        U.ROLE_CODE,
        R.ROLE_NAME
    FROM TB_USER_MASTER U
    LEFT JOIN TB_ROLE_MASTER R
      ON U.COMPANY_CODE = R.COMPANY_CODE
     AND U.ROLE_CODE   = R.ROLE_CODE
    WHERE U.COMPANY_CODE = #{companyCode}

    <if test="userName != null and userName != ''">
        AND U.USER_NAME LIKE '%' || #{userName} || '%'
    </if>

    <if test="dept != null and dept != ''">
        AND U.DEPT LIKE '%' || #{dept} || '%'
    </if>

    <if test="position != null and position != ''">
        AND U.POSITION LIKE '%' || #{position} || '%'
    </if>

    ORDER BY U.USER_ID
</select>


    <!-- ✅ 2) ROLE → MENU 권한 조회 -->
    <select id="selectRoleMenuAuthList" resultType="com.rootcore.auth.vo.RoleMenuAuthVO">
        SELECT
            A.COMPANY_CODE,
            A.ROLE_CODE,
            A.MENU_CODE,
            A.READ_YN,
            A.CREATE_YN,
            A.UPDATE_YN,
            A.DELETE_YN
        FROM TB_ROLE_MENU_AUTH A
        WHERE A.COMPANY_CODE = #{companyCode}
          AND A.ROLE_CODE    = #{roleCode}
        ORDER BY A.MENU_CODE
    </select>

    <!-- ✅ 3) ROLE → MENU 권한 전체 삭제 -->
    <delete id="deleteRoleMenuAuth">
        DELETE FROM TB_ROLE_MENU_AUTH
        WHERE COMPANY_CODE = #{companyCode}
          AND ROLE_CODE    = #{roleCode}
    </delete>

    <!-- ✅ 4) ROLE → MENU 권한 등록 -->
    <insert id="insertRoleMenuAuth">
        INSERT INTO TB_ROLE_MENU_AUTH (
            COMPANY_CODE,
            ROLE_CODE,
            MENU_CODE,
            READ_YN,
            CREATE_YN,
            UPDATE_YN,
            DELETE_YN
        ) VALUES (
            #{companyCode},
            #{roleCode},
            #{menuCode},
            #{readYn},
            #{createYn},
            #{updateYn},
            #{deleteYn}
        )
    </insert>

</mapper>
