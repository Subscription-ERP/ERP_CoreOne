<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.rootcore.auth.mapper.PasswordResetMapper">

    <!-- 1) USER_ID + EMAIL 확인 -->
    <select id="findUserByIdAndEmail" resultType="map">
        SELECT USER_ID,
               EMAIL,
               COMPANY_CODE
        FROM TB_USER_MASTER          
        WHERE USER_ID = #{userId}
          AND EMAIL  = #{email}
    </select>

    <!-- 2) 기존 토큰 무효화 -->
    <update id="invalidateOldToken">
        UPDATE TB_PASSWORD_RESET
        SET EXFIRED_DATE = SYSDATE - 1,
            UPDATED_BY   = 'SYSTEM',
            UPDATE_DATE  = SYSDATE
        WHERE USER_ID = #{userId}
    </update>

    <!-- 3) 새 토큰 INSERT -->
    <insert id="insertResetToken">
        INSERT INTO TB_PASSWORD_RESET (
            COMPANY_CODE,
            USER_ID,
            TOKEN_ID,
            TOKEN,
            EXFIRED_DATE,
            CREATED_BY,
            CREATE_DATE
        ) VALUES (
            #{companyCode},
            #{userId},
            SYS_GUID(),
            #{token},
            SYSDATE + (30/1440),  <!-- 30분 유효 -->
            'SYSTEM',
            SYSDATE
        )
    </insert>

    <!-- 4) 토큰으로 유효한 값 조회 (EXFIRED_DATE가 현재 이후인 경우만) -->
    <select id="findValidToken" resultType="map">
        SELECT COMPANY_CODE,
               USER_ID,
               TOKEN_ID,
               TOKEN,
               EXFIRED_DATE
        FROM TB_PASSWORD_RESET
        WHERE TOKEN = #{token}
          AND EXFIRED_DATE > SYSDATE
    </select>

    <!-- 5) USER 비밀번호 변경 -->
    <update id="updateUserPassword">
        UPDATE TB_USER_MASTER              
        SET PASSWORD   = #{password},
            UPDATED_BY = 'SYSTEM',
            UPDATE_DATE = SYSDATE
        WHERE USER_ID = #{userId}
    </update>

    <!-- 6) 토큰 사용 완료 처리 (바로 만료 시켜버리기) -->
    <update id="expireToken">
        UPDATE TB_PASSWORD_RESET
        SET EXFIRED_DATE = SYSDATE - 1,
            UPDATED_BY   = 'SYSTEM',
            UPDATE_DATE  = SYSDATE
        WHERE TOKEN = #{token}
    </update>

</mapper>
