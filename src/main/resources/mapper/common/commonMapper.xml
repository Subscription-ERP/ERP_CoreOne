<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rootcore.common.mapper.CommonMapper">
	<!-- 거래처유형 조회 -->
	<select id="selectType" resultType="CommonVO">
		SELECT cm_code.code,
			   cm_code.code_name
		FROM tb_cm_code cm_code JOIN tb_cm_code_group code_group
									 ON cm_code.group_code = code_group.group_code
		WHERE code_group.group_code = #{groupCode}
	</select>
	
	<!-- 공통코드 조회 -->
	<select id="selectCode" resultType="CommonVO">
		SELECT cm_code.code,
			   cm_code.code_name
		FROM tb_cm_code cm_code JOIN tb_cm_code_group code_group
									 ON cm_code.group_code = code_group.group_code
		WHERE code_group.group_code = #{code}
	</select>
	
	
	<!-- 공통코드 조회 -->
	<select id="selectAttributeCode" resultType="CommonVO">
		SELECT cm_code.code,
			   cm_code.code_name
		FROM tb_cm_code cm_code JOIN tb_cm_code_group code_group
									 ON cm_code.group_code = code_group.group_code
		WHERE code_group.group_code = #{groupCode}
		  AND cm_code.attribute01 = #{attribute}
	</select>
	
</mapper>