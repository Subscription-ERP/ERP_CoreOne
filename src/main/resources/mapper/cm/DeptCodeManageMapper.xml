<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rootcore.cm.mapper.DeptCodeManageMapper">
	<!-- ================= 부서코드관리 부서조회 ================= -->
	<select id="selectDeptCodeManage" resultType="DeptMasterVO"
		parameterType="DeptMasterVO">
		SELECT 	dm.dept_code,
				dm.company_code,
				dm.dept_name,
				dm.upper_dept_no,
				dmu.dept_name as upper_dept_name,
				dm.dept_level,
				dm.start_date,
				dm.end_date,
				ccs.code_name as status,
				dm.dept_mng,
				dm.rm
		FROM 	tb_dept_master dm
				JOIN tb_cm_code ccs
				ON ccs.group_code = '0R'
				LEFT JOIN tb_dept_master dmu
				ON dmu.dept_code = dm.upper_dept_no
		WHERE 	1=1
		<choose>
			<when test="companyCode == null or companyCode == ''">
				AND dm.company_code = '0000'
			</when>
			<otherwise>
				AND dm.company_code = #{companyCode}
			</otherwise>
		</choose>
			AND ccs.code = dm.status
		<if test="deptName != null and deptName != '' ">
			AND	dm.dept_code = #{deptName}	
		</if>
		<if test="upperDeptName != null and upperDeptName != '' ">
			AND	dmu.dept_code = #{upperDeptName}	
		</if>
		<if test="status != null and status != '' ">
			AND	ccs.code = #{status}	
		</if>
		<if test="deptCodeManageStartLevel != null and deptCodeManageStartLevel != '' ">
			AND	dm.dept_level >= #{deptCodeManageStartLevel}	
		</if>
		<if test="deptCodeManageEndLevel != null and deptCodeManageEndLevel != '' ">
			AND	dm.dept_level &lt;= #{deptCodeManageEndLevel}	
		</if>
	</select>
</mapper>